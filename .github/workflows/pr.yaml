name: PR Build
on:
  pull_request:
  workflow_dispatch:

permissions:
  id-token: write
  pull-requests: write

concurrency:
  group: terraform

jobs:
  plan:
    if: github.event.pull_request.head.repo.fork != true
    uses: curioswitch/cdktf-actions/.github/workflows/pr.yaml@2838afabaf55ce4155ce1934d06db14a340a3b99
    with:
      gcp_project_id: tasuke-dev
      gcp_project_number: 720364425367
